# Rust
/target
**/*.rs.bk

# Node / UI
node_modules/
dist/
.vite/

# Divers
.DS_Store
*.log

# Artefacts de mesure locaux
/tools/out/
# Rapports de `cargo mutants` — voir tools/README.md
/mutants.out/
/mutants.out.old/
/mutants.out.lock

# Arbitres construits localement (voir tools/setup-arbiters.sh)
/tools/arbiters/

# Résidus laissés par les arbitres de match
/config.json
/*.pgn

# Trace de déclenchement des hooks (voir tools/verify-hooks.sh)
/.claude/hooks-fired.log

# Adresse du carnet de bord — privé, hors dépôt par décision de Théo.
# Lu par tools/etat.sh pour que le pointeur soit mécanique sans que le dépôt
# porte le lien.
/.claude/carnet.local
